services:
  - type: web
    name: gct-assistant-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn gct_backend:app"
    envVars:
      - key: DATABASE_PATH
        value: /opt/render/project/src/gct_data.db
      - key: PORT
        value: 10000